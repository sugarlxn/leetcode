#include <iostream>
using namespace std;
int main(int argc,char * argv[]){
    //多组测试样例，每组输⼊数据为字符串，字符⽤空格分隔,输出
    //为⼩数点后两位
    string s;
    while (getline(cin,s)){
        //接收一整行字符串
        for(int i=0; i < s.size(); i++){
            //遍历字符串
        }
    }
    
    return 0;
}